# Vite 环境变量必须以 VITE_ 开头，src 内通过 import.meta.env.VITE_XXX 访问
# 复制为 .env 后填实际值（.env 不进 git）
VITE_API_BASE_URL=http://localhost:8081/api
# 后端 application-local.yml 的 api.security.api-key 一致；留空表示不发送（后端鉴权需同步关闭）
VITE_API_KEY=
